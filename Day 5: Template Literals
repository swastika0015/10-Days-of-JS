function sides(literals, ...expressions) {
    const [a,p] = expressions;
    const res = [];
    const root = Math.sqrt((p*p) -(16*a));
    const s1 = (p + root)/4;
    const s2 = (p - root)/4;
    res.push(s2);
    res.push(s1);
    return res.sort(); 
}
